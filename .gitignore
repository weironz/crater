# Rust
/target
**/*.rs.bk
*.pdb

# Crater artifacts
*.bundle
/dist

# scratch / debug logs
buildcheck.*
build.log
test.log
_*.txt
_*.log

# Editors / OS / tooling
# .claude/ 默认忽略,但项目级共享配置(settings.json / skills)入库;
# settings.local.json 等本机私有配置继续排除
.claude/*
!.claude/settings.json
!.claude/skills/
.idea/
.vscode/
.DS_Store
Thumbs.db

# 本地产物
/inventory.yaml
*.oci
*.tar
*.log

# 编辑器交换/临时文件
*.swp
*.swo
*~
# Added by code-review-graph
.code-review-graph/
.env

# UI 写回前留的旧版(误操作能拖回来),以及回收站 —— 都不该进版本库
*.bak
*.yaml.bak
.crater-trash/

# just dev / CI 打镜像用的临时二进制
/dist/
